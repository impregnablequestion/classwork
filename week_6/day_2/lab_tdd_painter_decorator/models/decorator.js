const Decorator = function () {
    this.stock = []
}

Decorator.prototype.add_paint = function(paint_can) {
    this.stock.push(paint_can);
}

Decorator.prototype.add_paint = function() {
    let total = 0
    
}

module.exports = Decorator;